apply plugin: 'com.android.application'

android {
    compileSdkVersion 26
    defaultConfig {
        applicationId "com.example.brackets"
        minSdkVersion 21
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation 'com.android.support:appcompat-v7:26.1.0'

//    implementation ('org.apache.httpcomponents:httpclient:4.5.3') // does not generate warning
    implementation 'org.apache.httpcomponents:httpclient:4.5.3' // generates warning
}
